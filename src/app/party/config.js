// Soft-launch switch for the Bonjour Party page, shared by the page metadata
// (robots noindex) and the sitemap. While false the page is fully built and
// SEO-ready but kept OUT of search indexes and OUT of the sitemap. Flip to
// `true` when the event is public — indexing turns on and /party is added to
// the sitemap automatically.
export const PARTY_INDEXABLE = true;

export const PARTY_URL = "https://www.bonjouridol.com/party";
